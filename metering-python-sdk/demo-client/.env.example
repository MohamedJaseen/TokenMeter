# Copy to `.env` and fill in your tenant's API key.
# Never commit the real `.env` — it holds a live billing credential.
METERING_API_KEY=key_456
# Optional: point at a different environment.
# METERING_BASE_URL=http://localhost:8083
